server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        default-label: main
        git:
          uri: https://github.com/isabelapiana/microservice-repo
          search-paths: diet, finance, admin, pacient
